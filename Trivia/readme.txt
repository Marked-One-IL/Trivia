Some functions are only available at newer versions of C++ eg std::format(), std::print(), std::println().

Go to:
Properties -> C/C++ -> Language -> C++ Language Standard

And then set it to the latest version.
